{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
    <h1>Edit Article</h1>

    {{ include('article/_form.html.twig', {'button_label': 'Update'}) }}
    <a class="btn btn-sm btn-primary" href="{{ path('app_article_delete_picture', { id: article.id }) }}">Remove picture</a>

    <a href="{{ path('app_article_index') }}" class="btn btn-primary">Back to list</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
